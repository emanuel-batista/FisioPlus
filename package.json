{
  "name": "fisioplus-pose-landmarker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:lite": "vite --mode lite",
    "dev:full": "vite --mode full",
    "dev:heavy": "vite --mode heavy",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "tunnel": "node server/control-server.js",
    "tunnel:public": "npx ngrok http 4010 --log=stdout"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  }
}
